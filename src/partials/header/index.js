import React, { useContext, useEffect, useState } from 'react';

const Start = (props) => {
    return (<LoadHeader />);
};

const LoadHeader = (props) => {
    return (
        <>
        </>
    );
};

export default Start;
