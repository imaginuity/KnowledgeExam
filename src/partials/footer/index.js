import React from 'react';

const Start = (props) => {
    return (<LoadFooter />);
};

const LoadFooter = (props) => {
    return (
        <div>
            <div>Footer</div>
        </div>
    );
};

export default Start;
